#include <cstdlib>
#include <cstdio>

#include "Server.h"

Server::Server() { }

Server::~Server() { }

int Server::start(int argc, char* argv[]) {
	printf("Terminal test. Press enter to exit.");
	
	getchar();
	
	return 0;
}